# QNS Design System (subset)

The tokens, component CSS and icons the QNS cockpit loads, served at `/design-system/`.
These files are copied from the full QNS Design System; change them there and re-export.
